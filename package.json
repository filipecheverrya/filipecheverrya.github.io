{
  "name": "with-rosetta",
  "version": "1.0.0",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "out": "next out",
    "deploy": "next build && next export && touch out/.nojekyll && git add out/ && git commit -m \"Deploy master\" && git subtree push --prefix out origin master"
  },
  "dependencies": {
    "next": "latest",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "rosetta": "1.0.0",
    "styled-components": "^5.1.1"
  },
  "license": "ISC",
  "devDependencies": {
    "babel-plugin-module-resolver": "^4.0.0",
    "babel-plugin-styled-components": "^1.10.7",
    "babel-plugin-transform-class-properties": "^6.24.1"
  }
}
